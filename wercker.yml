box: debian
build:
    steps:
        - script:
            name: initialize git submodules
            code: |
                cd themes/rlc-theme
                git submodule update --init --recursive
      - arjen/hugo-build:
        version: "0.15"
